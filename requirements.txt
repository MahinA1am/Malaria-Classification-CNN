tensorflow==2.14.0
tensorflow-gpu==2.14.0
keras==2.14.0
matplotlib==3.8.0
numpy==1.24.4
scikit-learn==1.3.0
pandas==2.1.0
jupyterlab==4.0.6
notebook==7.0.4

💡 Notes:

tensorflow-gpu==2.14.0 ensures compatibility with P100/T4 on CUDA 11.2–11.8.

If you're on Colab, you don’t need to install GPU drivers — it comes pre-installed.

Locally or on a VM, you’ll need NVIDIA drivers + CUDA installed properly.
